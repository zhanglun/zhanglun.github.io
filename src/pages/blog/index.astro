---
import Blog from "@layouts/Blog.astro";
import getSortedPosts from "@utils/getSortedPosts";
import { getCollection } from "astro:content";

const allPosts = getSortedPosts(await getCollection("blogs"));
---

<Blog allPosts={allPosts} />
